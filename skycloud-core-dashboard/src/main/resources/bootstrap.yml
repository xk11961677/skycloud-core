server:
  port: 8789
spring:
  application:
    name: dashboard

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/
#  instance:
#    preferIpAddress: true
#    ipAddress:  127.0.0.1


management:
  security:
    enabled: false
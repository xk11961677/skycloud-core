spring:
  application:
    name: zipkin

server:
  port: 9411
  

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_SERVICE_URL:http://localhost:8761}/eureka/
#  instance:
#    preferIpAddress: true
#    ipAddress:  127.0.0.1

management:
  security:
    enabled: false
server:
  port: 8761
spring:
  application:
    name: eureka
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL:http://localhost:8888}/config
      name: eureka
      profile: default

management:
  security:
    enabled: false